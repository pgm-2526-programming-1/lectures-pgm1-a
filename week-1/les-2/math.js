const a = 10;
const b = 20;

const sum = a + b;

// Seppe heeft 3 appels
let apples = 3;
// Seppe eet een appel
apples = apples - 1;
console.log(apples);
// Seppe eet nog een appel (andere manier van schrijven)
apples -= 1;
console.log(apples);
// Seppe gaat naar de winkel en koopt 3 appels
apples = apples + 3;
// je kan ook schrijven
// apples += 3;
console.log(apples);
// Seppe eet opnieuw een appel
apples--; // verminder apples met 1 (hetzelfde als apples = apples - 1;)
console.log(apples);
// Seppe gaat naar de winkel en koopt 1 appel
apples++;
console.log(apples);
